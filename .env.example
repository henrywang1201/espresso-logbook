# Supabase 连接信息（Project Settings → API 里复制）
# 本地：复制本文件为 .env.local 并填入真实值。
# Vercel：在 Project Settings → Environment Variables 配置同名变量。
# 注意 anon key 是公开密钥（随前端打包），访问权限由数据库 RLS 策略控制。
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
